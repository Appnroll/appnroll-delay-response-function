{
  "name": "@appnroll/appnroll-delay-response-function",
  "version": "1.0.0",
  "description": "Delays async function to be resolved not earlier than after given time.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prepublish": "npm run build",
    "test": "jest --config jest.config.json",
    "format": "prettier --write \"src/**/*.ts\"",
    "prepare": "npm run build",
    "prepublishOnly": "npm test",
    "version": "npm run format && git add -A src",
    "postversion": "git push && git push --tags"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Appnroll/appnroll-delay-response-function.git"
  },
  "keywords": [
    "delay",
    "response"
  ],
  "author": "@appnroll",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/Appnroll/appnroll-delay-response-function/issues"
  },
  "homepage": "https://github.com/Appnroll/appnroll-delay-response-function#readme",
  "devDependencies": {
    "@types/jest": "^26.0.4",
    "codecov": "^3.7.1",
    "jest": "^26.1.0",
    "prettier": "^2.0.5",
    "ts-jest": "^26.1.3",
    "typescript": "^3.9.3"
  },
  "files": [
    "dist/**/*"
  ]
}
